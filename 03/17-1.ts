// 函数
function add(x, y) {
    return x + y
}

let myAdd = function (x, y) {
    return x + y
}

let myAdd1 = function add1(x, y) {
    return x + y
}


let z = 100
function addToZ(x, y) {
    return x + y + z
}